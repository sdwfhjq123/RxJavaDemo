<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.yinhao.rxjavademo">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity"></activity>
        <activity android:name=".get.TranslationActivity"></activity>
        <activity android:name=".post.C2ETranslationActivity"></activity>
        <activity android:name=".polling.PollingActivity"></activity>
        <activity android:name=".threeLevelCache.ThreeLevelCacheActivity"></activity>
        <activity android:name=".merge.MergeActivity"></activity>
        <activity android:name=".merge.ZipAcitivity">

        </activity>
        <activity android:name=".merge.CombineLatestActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>